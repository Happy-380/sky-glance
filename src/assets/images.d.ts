// Helps TypeScript recognize these asset imports in strict mode.
// (vite/client already declares *.png / *.svg; kept here so both tsc and
// IDE plugin tooling find the declarations regardless of config order.)
/// <reference types="vite/client" />
